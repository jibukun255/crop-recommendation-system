services:
  - type: web
    name: crop-recommendation-system
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir src 05_app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
